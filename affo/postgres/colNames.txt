CREATE TABLE IF NOT EXISTS colNames 
(id SERIAL PRIMARY KEY NOT NULL,
leader INTEGER NOT NULL,
description VARCHAR(240),
type VARCHAR(12) NOT NULL,
flag VARCHAR(40),
name VARCHAR(100) NOT NULL UNIQUE);