-- It is attached to the wars table so there is no need to store attacker,defender ids
CREATE TABLE 'peace_offer' (
'id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
'message' TEXT,

// These the user can demand
'money' INTEGER DEFAULT 0,
'ammunition' INTEGER DEFAULT 0,

);
